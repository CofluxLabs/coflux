version: "3.7"

services:
  repo_one:
    build: "."
    volumes:
      - ".:/app"
      - "../../clients/python:/packages"
    environment:
      PROJECT: project_1
      ENVIRONMENT: dev/one
      HOST: server:7070
      MODULE: app.repo
      VERSION: 1
    init: true

  repo_two:
    build: "."
    volumes:
      - ".:/app"
      - "../../clients/python:/packages"
    environment:
      PROJECT: project_1
      ENVIRONMENT: dev/two
      HOST: server:7070
      MODULE: app.repo
      VERSION: 1
    init: true

networks:
  default:
    name: coflux
