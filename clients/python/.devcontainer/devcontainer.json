{
	"name": "Clients → Python",

	"build": {
		"dockerfile": "Dockerfile",
		"context": "..",
		"args": { "VARIANT": "3.9" }
	},

	"extensions": [
		"ms-python.python",
		"ms-python.vscode-pylance"
	],

	"postCreateCommand": "pip3 install --user -r requirements.txt",

	"remoteUser": "vscode"
}
